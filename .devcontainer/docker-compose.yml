version: '3'
services:
  devcontainer:
    image: mcr.microsoft.com/devcontainers/python:0-3.9
    volumes:
      - ../:/workspace
      - ~/.aws:/home/vscode/.aws
    command: sh -c "while sleep 1000; do :; done"
