"""setup script for artifact_downloader package"""
from setuptools import setup

setup(name="artifact_downloader",
      version="1.0",
      # list folders, not files
      packages=["artifact_downloader"],
      )
