excludes:
  paths:
    - pattern: ".github/**"
      reason: "BUILD_TOOL_OF"
      description: "Github actions and workflows are not included in a build"
    - pattern: ".vscode/**"
      reason: "BUILD_TOOL_OF"
      description: "Configuration for developer environment only"
    - pattern: "**/tests/**"
      reason: "TEST_OF"
      description: "Tests of the micro services"
  scopes:
    - pattern: "devDependencies"
      reason: "DEV_DEPENDENCY_OF"
      comment: "Packages for development only."
