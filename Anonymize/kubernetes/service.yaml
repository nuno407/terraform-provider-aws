apiVersion: v1
kind: Service
metadata:
    name: anonymize-service
    namespace: pem3brg
spec:
    type: LoadBalancer
    selector:
        app: anonymize-image
    ports:
        - name: http
          port: 8080
          targetPort: 80
          protocol: TCP
